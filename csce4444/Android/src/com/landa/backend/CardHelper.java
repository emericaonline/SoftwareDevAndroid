package com.landa.backend;

import com.landa.db.orderOpenHelper;
import com.landa.helpers.AsyncHelper;

public class CardHelper {

	public static void update() {
		// need a way to get and Id of the card that was removed.

	/*	AsyncHelper.setURL("http://192.184.85.36/csce4444/android/");
		orderOpenHelper db = new orderOpenHelper(AsyncHelper.getContext());

		asyncLoader task = new asyncLoader();
		task.execute(); // to start the connection process.*/
	}

}
